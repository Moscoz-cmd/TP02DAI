 const pi = 3.14159265359;

 function suma(a, b) {
  return a + b;
}

 const resta = (a, b) => a - b;

 function multiplicar(a, b) {
  return a * b;
}

 const dividir = (a, b) => (b === 0 ? NaN : a / b);
export {pi,suma,resta,multiplicar,dividir};